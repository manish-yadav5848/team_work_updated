from pyspark.sql.types import StructType, StructField, StringType, DateType, IntegerType

schema = StructType([
    StructField('client_id', StringType(), True), 
    StructField('plan_number', StringType(), True), 
    StructField('source_cycle_date', StringType(), True), 
    StructField('total_enrollments', StringType(), True), 
    StructField('total_employee_before_tax_contributions_count', StringType(), True), 
    StructField('total_employee_after_tax_contributions_count', StringType(), True), 
    StructField('total_employer_contributions_count', StringType(), True), 
    StructField('total_rollover_contributions_count', StringType(), True), 
    StructField('total_termination_distributions_with_rollover_count', StringType(), True), 
    StructField('total_termination_distributions_with_rollover_rolled_amount', StringType(), True),
    StructField('total_termination_distributions_with_rollover_not_rolled_amount', StringType(), True), 
    StructField('total_termination_distributions_with_no_rollover_count', StringType(), True), 
    StructField('total_termination_distributions_with_no_rollover_amount', StringType(), True), 
    StructField('total_in_service_withdrawals_with_rollover_count', StringType(), True), 
    StructField('total_in_service_withdrawals_with_rollover_rolled_amount', StringType(), True), 
    StructField('total_in_service_withdrawals_with_rollover_not_rolled_amount', StringType(), True), 
    StructField('total_in_service_withdrawals_with_no_rollover_count', StringType(), True), 
    StructField('total_in_service_withdrawals_with_no_rollover_amount', StringType(), True), 
    StructField('total_hardship_withdrawals_count', StringType(), True), 
    StructField('total_hardship_withdrawals_amount', StringType(), True), 
    StructField('total_installments_setup_count', StringType(), True), 
    StructField('total_installment_distributions_by_check_count', StringType(), True), 
    StructField('total_installment_distributions_by_check_amount', StringType(), True),
     StructField('total_installment_distributions_by_ach_count', StringType(), True), 
     StructField('total_installment_distributions_by_ach_amount', StringType(), True), 
     StructField('total_default_rollover_distributions_count', StringType(), True), 
     StructField('total_default_rollover_distributions_amount', StringType(), True), 
     StructField('total_deminimus_distributions_count', StringType(), True), 
     StructField('total_deminimus_distributions_amount', StringType(), True), 
     StructField('total_loan_issue_count', StringType(), True), 
     StructField('total_loan_reamortized_count', StringType(), True), 
     StructField('total_loan_defaults_deemed_distributions_count', StringType(), True), 
     StructField('total_loan_repayments_count', StringType(), True), 
     StructField('total_loan_payoff_count', StringType(), True), 
     StructField('total_dividend_pass_thru_count', StringType(), True), 
     StructField('total_dividend_pass_thru_amount', StringType(), True), 
     StructField('total_dividend_reinvestments_count', StringType(), True), 
     StructField('total_dividend_reinvestments_amount', StringType(), True), 
     StructField('total_rebalance_transfers_count', StringType(), True), 
     StructField('total_fee_deductions_count', StringType(), True), 
     StructField('total_qdro_splits_count', StringType(), True), 
     StructField('total_beneficiary_splits_count', StringType(), True), 
     StructField('total_rmd_count', StringType(), True), 
     StructField('total_rmd_amount', StringType(), True), 
     StructField('filler', StringType(), True), 
     StructField('total_loan_issue_amount', StringType(), True)])
